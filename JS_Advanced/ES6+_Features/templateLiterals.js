//Template Literals (Şablon Metinler)

//TR
//Karmaşık string birleştirmeleri kolaylaştırır.
//Backtick (`) ile yazılır, ${} ile değişken yerleştirilir.

//EN
//It simplifies complex string concatenations.
//It is written with a backtick (`) and placed in variables with ${}.

const name = "Uğur";
const age = 25;

const message = `My name is ${name} and I’m ${age} years old.`;
console.log(message);

//Multi-line string writing (Çok satırlı string yazımı)
const info = `
Line 1
Line 2
Line 3
`;
console.log(info);
